global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  # Scrape your FastAPI service inside Docker
  - job_name: "recruitair-extractor-api"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "extractor-api:8000"